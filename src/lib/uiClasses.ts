/** Shared field styles for modal / overlay forms (light + dark). */
export const modalFieldClass =
  'gl-modal-field w-full rounded-2xl border px-4 py-3 text-sm text-[var(--tf-ink)] outline-none transition duration-200'

export const modalLabelClass = 'text-xs font-semibold tracking-wide text-[var(--tf-ink-muted)]'
